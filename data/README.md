# Data Directory

Local market data lives here but is not committed.

- `data/raw/` - optional local raw copies.
- `data/processed/` - normalized outputs generated by scripts.

Raw source files remain the source of truth and are registered in `wiki/reference/data-inventory.md`.
